import{registerBlockType}from'@wordpress/blocks';import Edit from'./edit';import metadata from'./block.json';import{questionBlockIcon}from"../component/icon";registerBlockType(metadata.name,{icon:questionBlockIcon,edit:Edit,__experimentalLabel(attributes,{context}){const{title}=attributes;const customName=attributes?.metadata?.name;const hasContent=title?.length>0;if(context==='list-view'&&(customName||hasContent)){return customName||title;}},});